1. Install required packages from requirements.txt
2. Run docker compose to create the two images that will be needed, i.e. postgres image with timeslot database. Run the two images.
3. Create .env file with groq api key, postgres server details etc.
4. Open the frontend using frontend/index.html and proceed.
